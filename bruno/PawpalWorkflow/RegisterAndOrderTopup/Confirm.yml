info:
  name: Confirm
  type: http
  seq: 3

http:
  method: PATCH
  url: http://localhost:8000/topup/{{chargeId}}
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: chargeId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
