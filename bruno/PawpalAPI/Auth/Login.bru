meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:8000/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
