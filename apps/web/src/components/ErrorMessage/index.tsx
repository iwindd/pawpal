import { ErrorMessage } from "@pawpal/ui/core";

/**
 * ErrorMessage component
 * @description A component that displays an error message
 * @returns {JSX.Element} The ErrorMessage component
 */
export default ErrorMessage;
