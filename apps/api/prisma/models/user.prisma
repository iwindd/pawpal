model User {
  id          String   @id @default(cuid())
  email       String   @unique
  displayName String
  password    String
  avatar      String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  userWallets                     UserWallet[]            @relation("UserWallets")
  orders                          Order[]                 @relation("OrderUser")
  roles                           Role[]                  @relation("UserRoles")
  Resources                       Resource[]              @relation("ResourceUser")
  carousels                       Carousel[]              @relation("CarouselCreatedBy")
  fields                          ProductField[]          @relation("FieldCreatedBy")
  processedOrders                 Order[]                 @relation("OrderProcessedBy")
  processedUserWalletTransactions UserWalletTransaction[] @relation("UserWalletTransactionProcessedBy")

  @@map("users")
}

model Session {
  id        String   @id
  sid       String   @unique
  data      String
  expiresAt DateTime
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("sessions")
}
